# Generated FFI bindings

Generated by `diplomat-tool cpp` (0.15.x) from `../../rust/src/bridge.rs`:

```sh
cd ../../rust && diplomat-tool cpp ../src/diplomat
```

These headers are an implementation detail of the dashpkc wrapper
(`../*.cpp`); they are not installed and must not be included from
public headers. Local modifications are permitted but should be
kept minimal so regeneration stays reviewable.

[package]
name = "dash-pkc"
version = "0.0.0"
edition = "2021"
license = "MIT"

[[bench]]
name = "pkc"
path = "bench/main.rs"
harness = false
required-features = ["test"]

[dependencies]
base58ck = { version = "0.4", default-features = false, features = ["alloc"] }
bitcoin-consensus-encoding = { version = "0.2", default-features = false, features = [
  "alloc",
] }
bitcoin_hashes = { version = "0.20", default-features = false, features = [
  "alloc",
] }
aes = { version = "0.8", default-features = false, optional = true }
blst = { version = "0.3", default-features = false, optional = true }
cfg-if = "1"
dash-num = { version = "0.0.0", path = "../num" }
dash-types = { version = "0.0.0", path = "../types", default-features = false }
hex-conservative = { version = "0.3", default-features = false, features = [
  "alloc",
] }
hex-literal = "0.4"
k256 = { version = "0.13", default-features = false, features = [
  "arithmetic",
  "ecdsa",
  "sha256",
], optional = true }
rand_core = { version = "0.6", default-features = false }
rayon = { version = "1", optional = true }
serde = { version = "1", default-features = false, features = [
  "alloc",
  "derive",
], optional = true }
sha2 = { version = "0.10", default-features = false, optional = true }
subtle = { version = "2", default-features = false }
zeroize = { version = "1", default-features = false, features = ["alloc", "derive"] }

[dev-dependencies]
dash-dev = { version = "0.0.0", path = "../dev", features = ["full"] }
divan = "0.1"
rand_core = { version = "0.6", features = ["getrandom"] }
rstest = "0.25"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = []
std = [
  "base58ck/std",
  "bitcoin_hashes/std",
  "dash-types/std",
  "dep:rayon",
  "hex-conservative/std",
  "rand_core/getrandom",
]
bls = ["dep:blst", "dep:sha2", "dep:aes"]
# Portable blst backend. Without this, blst's build script enables
# ADX iff the BUILD host supports it, so x86_64 artifacts built on
# modern CI SIGILL on pre-Broadwell CPUs. Costs ~20-30% on x86_64
# field ops; no effect on aarch64.
bls-portable = ["bls", "blst/portable"]
k256 = ["dep:k256"]
serde = ["dep:serde", "dash-types/serde"]
full = ["k256", "bls", "serde", "std"]
test = ["full"]
_internal = []

[lints]
workspace = true

[package.rust-version]
workspace = true

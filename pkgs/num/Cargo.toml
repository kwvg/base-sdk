[package]
name = "dash-num"
version = "0.0.0"
edition = "2021"
license = "MIT"

[features]
default = []
std = ["bitcoin-consensus-encoding/std", "dash-types/std"]
full = ["std", "serde"]
serde = ["dep:serde", "dash-types/serde"]
_internal = []

[dependencies]
bitcoin-consensus-encoding = { version = "0.2", default-features = false }
dash-types = { version = "0.0.0", path = "../types", default-features = false }
serde = { version = "1", default-features = false, features = [
  "derive",
  "alloc",
], optional = true }

[dev-dependencies]
divan = "0.1"
hex-literal = "0.4"
rstest = "0.25"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[test]]
name = "hash"
path = "tests/hash.rs"

[[test]]
name = "arith"
path = "tests/arith.rs"

[[test]]
name = "compact"
path = "tests/compact.rs"

[[test]]
name = "serde"
path = "tests/serde.rs"
required-features = ["serde"]

[[bench]]
name = "num"
path = "bench/main.rs"
harness = false

[lints]
workspace = true

[package.rust-version]
workspace = true
